# Требования для запуска проекта

## ❌ Текущая ситуация

На вашем компьютере не установлены необходимые инструменты:
- ❌ Node.js и npm (для frontend)
- ❌ Python и pip (для backend)

## 📦 Что нужно установить

### 1. Node.js (для Frontend)

**Скачать и установить:**
- Перейдите на https://nodejs.org/
- Скачайте LTS версию (рекомендуется)
- Установите с настройками по умолчанию
- После установки перезапустите терминал

**Проверка установки:**
```powershell
node --version
npm --version
```

### 2. Python (для Backend)

**Вариант 1: Официальный Python**
- Перейдите на https://www.python.org/downloads/
- Скачайте Python 3.11 или новее
- **Важно:** При установке отметьте "Add Python to PATH"
- Установите Python

**Вариант 2: Через Microsoft Store**
- Откройте Microsoft Store
- Найдите "Python 3.11" или новее
- Установите

**Проверка установки:**
```powershell
python --version
pip --version
```

## 🚀 После установки

После установки Node.js и Python, выполните:

### Frontend:
```powershell
cd frontend
npm install
```

### Backend:
```powershell
cd backend
pip install -r requirements.txt
```

### Генерация JWT ключа:
```powershell
cd backend
python generate_jwt_secret.py
```
Скопируйте сгенерированный ключ в `backend/.env` (замените `JWT_SECRET_KEY`)

### Запуск:

**Backend (терминал 1):**
```powershell
cd backend
uvicorn app.main:app --reload
```

**Frontend (терминал 2):**
```powershell
cd frontend
npm run dev
```

## ⚡ Быстрая установка (если есть Chocolatey)

Если у вас установлен Chocolatey, можно установить все одной командой:

```powershell
choco install nodejs python
```

## 📝 Альтернатива: Использовать готовые образы

Если не хотите устанавливать локально, можно использовать:
- **Docker** (если установлен)
- **GitHub Codespaces** (онлайн среда разработки)
- **Vercel** (деплой без локальной установки)

## ✅ Проверка готовности

После установки всех инструментов, проверьте:

```powershell
node --version    # Должна быть версия 18+
npm --version     # Должна быть версия 9+
python --version  # Должна быть версия 3.11+
pip --version     # Должна быть версия 23+
```

Если все команды работают - можно устанавливать зависимости проекта!

