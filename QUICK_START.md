# Быстрый старт проекта

## ✅ Что уже сделано

1. ✅ Frontend зависимости установлены (`npm install`)
2. ✅ Backend виртуальное окружение создано
3. ✅ JWT Secret Key сгенерирован и обновлен в `.env`
4. ✅ Конфигурация Supabase настроена

## 🚀 Запуск проекта

### Backend (терминал 1):

```powershell
cd backend
.\venv\Scripts\python.exe -m uvicorn app.main:app --reload
```

Или если виртуальное окружение активировано:
```powershell
cd backend
.\venv\Scripts\Activate.ps1
uvicorn app.main:app --reload
```

Backend будет доступен на: `http://localhost:8000`

### Frontend (терминал 2):

```powershell
cd frontend
npm run dev
```

Frontend будет доступен на: `http://localhost:3000` (или другой порт из vite.config.ts)

## 🔍 Проверка работы

1. **Backend Health Check:**
   - Откройте: `http://localhost:8000/health`
   - Должно вернуть: `{"status": "ok"}`

2. **API Documentation:**
   - Откройте: `http://localhost:8000/docs`
   - Swagger UI с документацией API

3. **Frontend:**
   - Откройте: `http://localhost:3000`
   - Должна загрузиться главная страница

## 📝 Примечания

- Если виртуальное окружение не активируется из-за политики PowerShell, используйте прямой путь: `.\venv\Scripts\python.exe`
- Для активации venv может потребоваться: `Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser`
- JWT Secret Key уже сгенерирован и сохранен в `backend/.env`

## 🐛 Решение проблем

### Backend не запускается:
- Убедитесь, что используете Python из venv: `.\venv\Scripts\python.exe`
- Проверьте, что все пакеты установлены: `.\venv\Scripts\python.exe -m pip list`

### Frontend не запускается:
- Убедитесь, что Node.js установлен: `node --version`
- Переустановите зависимости: `npm install`

### Ошибки подключения к БД:
- Проверьте `backend/.env` - все переменные должны быть заполнены
- Убедитесь, что пароль в DATABASE_URL правильно закодирован

